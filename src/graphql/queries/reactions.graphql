query tweetReactions($tweetId: ID!) {
  tweet(id: $tweetId) {
    reactions {
      user {
        handle
        avatar
        data {
          avatarIsNFT
        }
      }
      emojiId
      id
      isMine
    }
  }
}
