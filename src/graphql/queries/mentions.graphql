query mentions($offset: Int, $limit: Int) {
  mentions(offset: $offset, limit: $limit) {
    id
    type
    tweet {
      ...TweetData
    }
    user {
      ...BaseUser
    }
    common {
      emojiId
      emojiUserId
    }
    createdAt
  }
}

query newMentions {
  newMentions {
    id
    tweet {
      id
    }
  }
}
